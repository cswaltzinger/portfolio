# CLI Data store 

A lightweight executable file that can be used to store/retrieve sensitive data in a project.  

### Example usage

```bash
GOOGLE_GEMENI_API_KEY=$(data --get api-keys gemeni )
...
```