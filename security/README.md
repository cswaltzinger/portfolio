# Security 

This repository contains some utility scripts that can be used in the docker image generated by the ./Dockerfile file
It allows for a more high-level use of the netcat and openssl utilities.  